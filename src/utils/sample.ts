export function add(props: { a: number; b: number }) {
  const { a, b } = props;
  return a + b;
}

export function minus(props: { a: number; b: number }) {
  const { a, b } = props;
  return a - b;
}
